<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="progressBarPadding">0dip</item>
        <item name="progressBarMy">@android:style/Widget.Holo.ProgressBar</item>
    </style>

    <style name="subject_text" parent="android:TextAppearance.Holo.Large">
        <item name="android:textSize">@dimen/subject_text_size</item>
        <item name="android:textColor">@color/subject_text</item>
    </style>

    <style name="content_text" parent="android:TextAppearance.Holo.Small">
        <item name="android:textSize">@dimen/content_text_size</item>
        <item name="android:textColor">@color/content_text</item>
    </style>

</resources>
